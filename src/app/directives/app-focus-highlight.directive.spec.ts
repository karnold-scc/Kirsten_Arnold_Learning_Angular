import { AppFocusHighlightDirective } from './app-focus-highlight.directive';

describe('AppFocusHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new AppFocusHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
